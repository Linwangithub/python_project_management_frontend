/**
 * Dialog copy configuration.
 *
 * Purpose:
 * - Keep simple dialog titles, buttons, and fixed option text in one place.
 * - Avoid duplicated hard-coded strings in Vue templates.
 */
export const dialogCommonText = {
  cancel: '取消',
  confirm: '确认',
  close: '关闭',
  confirmCreate: '确认创建',
}

/** Export project dialog text. */
export const copyProjectDialogText = {
  title: '复制项目',
  confirm: '确认复制',
}

/** Create user dialog text. */
export const exportProjectDialogText = {
  title: '导出项目',
  confirm: '确认导出',
}

/** Create server dialog text. */
export const createServerDialogText = {
  title: '新增服务器',
}

/** Project tool dialog text. */
export const createUserDialogText = {
  title: '创建账号',
}

/** Delete user dialog text. */
export const projectToolDialogText = {
  projectLabel: '项目',
}

/** Copy project dialog text. */
export const deleteUserDialogText = {
  migrateYes: '删除账号并迁移其项目到我的目录',
  migrateNo: '仅删除账号，不迁移项目',
}


/** Create Conda environment dialog text. */
export const createEnvDialogText = {
  title: '创建环境',
}

/** Create terminal session dialog text. */
export const createSessionDialogText = {
  title: '创建会话',
}


/** Server user management dialog text. */
export const serverUserDialogText = {
  addTitle: '增加用户',
  deleteTitle: '删除用户',
}
